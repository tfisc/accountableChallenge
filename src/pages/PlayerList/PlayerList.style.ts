import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    paddingTop: 16,
    marginBottom: 112,
    paddingHorizontal: 8,
  },
  centered: {
    flex: 1,
    alignSelf: 'center',
    justifyContent: 'center',
  },
});
